# GitHub OAuth App Client ID used by the Device Flow.
# Copy this file to `.env` and set your own Client ID.
# Build-time precedence: shell env var > `.env` > src/constants.ts fallback.
CLIENT_ID=Ov23liYOUR_ACTUAL_ID